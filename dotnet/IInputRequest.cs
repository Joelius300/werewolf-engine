namespace WerewolfEngine;

public interface IInputRequest
{
    // could maybe produce a generic request model but that would work best with
    // discriminated/tagged (idk) unions which are just rust enums and hard to implement in c#
}